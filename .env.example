# Path to Anthropic claude CLI (Claude Code). Used to consume the Max
# subscription quota instead of paying per-token API. Leave empty to
# auto-detect from PATH; set explicitly if installed in a custom location.
CLAUDE_CLI_PATH=
CLAUDE_MODEL=sonnet
